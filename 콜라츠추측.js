function solution(num) {
  var answer = 0;
  // 숫자가 짝수이면 나누어서 떨어진 값구하기
  // 숫자가 홀수이면 3을 곱하고 1을 더한 값구하기
  // 위에를 쭉 반복해서 1이 될때까지 반복하기
  // 반복은 500번까지만
  for (let i = 0; i <= 500; i++) {}
  return answer;
}
